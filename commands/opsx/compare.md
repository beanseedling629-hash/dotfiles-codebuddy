---
description: Compare technical/UX/interaction approaches side-by-side
argument-hint: topic or feature to compare
---

Load the openspec skill, then execute the `/opsx:compare` command with the provided arguments.

Compare multiple implementation approaches with a concise comparison matrix, clear tradeoffs, and a recommendation. Output is kept brief (~200 lines max) to minimize token usage.
