---
description: Condensed exploration with ~60% less output than explore
argument-hint: topic to explore
---

Load the openspec skill, then execute the `/opsx:explore-brief` command with the provided arguments.

Quick investigation mode that surfaces key findings, brief assessment, and next step recommendation. Max ~100 lines output. Use `/opsx:explore` for deeper investigation.
