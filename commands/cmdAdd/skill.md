---
description: "Install or scaffold a new CodeBuddy skill"
argument-hint: "[skill-name-or-url]"
---

Load the codebuddy-cmdAdd skill, then execute the `/cmdAdd:skill` command with the provided arguments.

Scaffolds a new skill with correct directory structure (skill.json, commands/, main prompt) or installs from a URL/repo.
