---
description: "Diagnose command/skill registration issues"
argument-hint: "[command-or-skill-name]"
---

Load the codebuddy-cmdAdd skill, then execute the `/cmdAdd:check` command with the provided arguments.

Scans all registration points and runs a diagnostic checklist for common pitfalls (colon-in-filename, missing frontmatter, orphan registrations, etc.). Offers auto-fix.
