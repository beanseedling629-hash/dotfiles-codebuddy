---
description: "Add a new slash command (handles subdirectory structure & dual registration)"
argument-hint: "[command-name] [description]"
---

Load the codebuddy-cmdAdd skill, then execute the `/cmdAdd:cmd` command with the provided arguments.

Creates a correctly structured slash command file, avoiding common pitfalls like colon-in-filename. Supports dual registration (IDE autocomplete + skill-internal).
